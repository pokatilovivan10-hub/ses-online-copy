# Keep directory listings disabled. HTTPS will be enabled only after the
# certificate for сэс.site has been issued on the web server.
Options -Indexes

RewriteEngine On
RewriteRule ^thanks/?$ thanks.html [L]
